#include <iostream>

using namespace std;
#include <fstream>
#include <string.h>

#include "suchheader.h"

/*void ClSuche::sucheInXml(char eingabe[64] ,ClXml *daten, char zustand){

    SuchErgeb = daten->durchsuche(eingabe, zustand);
    if(SuchErgeb==NULL){cout<<"HE";return;}
    else{
        Naechster = new ClSuche();
        daten= SuchErgeb->getNaechster();
        Naechster->sucheInXml(eingabe, daten, zustand);
    }
}

void ClSuche::zeigeSuche(){
    cout<< "Name: " << SuchErgeb->getVorname();
}*/
